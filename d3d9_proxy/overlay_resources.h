#pragma once

#define IDR_OVERLAY_FONT 101
// Shippori Mincho Bold: matches the vanilla settings labels, which are Mincho
// (extracted from data/opt.bin). Used for the in-game menus only.
#define IDR_MENU_FONT    102
